---
/**
 * Prose - Alias for Wrapper with prose variant
 * Used in MDX docs for styled text content
 */
import Wrapper from "@/components/fundations/containers/Wrapper.astro";

export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<Wrapper variant="prose" class={className}>
  <slot />
</Wrapper>
